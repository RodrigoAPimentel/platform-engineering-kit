# _temp Staging Area

`_temp/` is reserved for temporary and in-progress staging artifacts.

## Usage

- Keep this directory free of production-ready or operational source files.
- Move validated assets to canonical repository locations before merge.
- Do not delete this directory; keep it available as a staging area.
