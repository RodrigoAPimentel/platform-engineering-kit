# Install Resources

Static resources used by install scripts.

## Contents

- `p10k-zsh-plugin-configuration.txt` -> Base Powerlevel10k configuration template consumed by `install-oh-my-zsh-ubuntu.sh`.
- `standalone/devops-tools-stack/` -> Compose/templates and configuration assets consumed by `scripts/install/standalone/install-devops-tools-stack-standalone.sh`.

## Purpose

Keep install-time assets versioned close to the scripts that consume them.
